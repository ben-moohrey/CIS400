[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]
ruff = "*"

[requires]
python_version = "3.10"

[scripts]
testscriptfile = "echo 'what'"
lint = "ruff --format=github --target-version=py310 ."
